my_list = [1,2,3,4,5]



# append - adds an element to the end of the list
# pop - removes an element at a specified index (default = last)
# clear - clears the list
# index - returns the index of the specified element (ValueError if element not present)
# count - counts the occurences of the specified element. 0 if element not present
# insert - inserts an element at the specified index
# sort - sorts the list (DATATYPE MUST BE THE SAME or COMPATIBLE)
# reverse - reverses the list
# extend - it extends the list from a given iterable
# copy - it make a copy of the list
